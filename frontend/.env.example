# ── HMS Frontend — Environment Variables ─────────────────────────────────────
# For local development, defaults to http://localhost:8000/api/v1 (no .env needed)
# Set this in Vercel/Render for production builds.

VITE_API_URL=http://localhost:8000/api/v1
