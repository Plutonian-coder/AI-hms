# ── Backend API URL ───────────────────────────────────────────────────────────
# Set this to your Render backend URL in Vercel environment variables
# For local development, defaults to http://localhost:8000/api/v1 (no .env needed)
VITE_API_URL=https://your-hms-backend.onrender.com/api/v1
